# DEMO-1 — Hello world backend

Minimal Express service exposing `GET /hello`, which responds with:

```json
{ "message": "Hello, world!" }
```

## Run

```bash
npm install
npm start
```

Then open `http://localhost:3000/hello`.

## Test

```bash
npm test
```
